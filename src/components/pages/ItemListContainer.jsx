import { ProductCard } from "../common/ProductCard"

 const ItemListContainer = ({greeting}) => {
    return (<div>

<h4 style={{fontSize:"2rem", color: "steelblue"}}>{greeting}</h4>
 </div>
    )
}

export default ItemListContainer